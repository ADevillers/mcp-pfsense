[tool.poetry]
name = "mcp-pfsense"
version = "1.0.0"
description = "Read-only MCP server for the pfSense REST API (pfrest)"
authors = ["ADevillers"]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/ADevillers/mcp-pfsense"
repository = "https://github.com/ADevillers/mcp-pfsense"
packages = [{ include = "mcp_pfsense", from = "src" }]
keywords = ["mcp", "pfsense", "homelab", "pfrest"]
classifiers = [
    "Development Status :: 5 - Production/Stable",

    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Systems Administration",
]

[tool.poetry.dependencies]
python = "^3.11"
httpx = ">=0.27"
mcp = ">=2.2,<3"
pydantic = ">=2.0"
pydantic-settings = ">=2.0"

[tool.poetry.group.dev.dependencies]
pytest = ">=8.0"
pytest-asyncio = ">=0.24"
respx = ">=0.21"
ruff = ">=0.8"

[tool.poetry.scripts]
mcp-pfsense = "mcp_pfsense.server:main"
mcp-pfsense-check = "mcp_pfsense.check:main"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
